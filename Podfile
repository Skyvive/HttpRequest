# Uncomment this line to define a global platform for your project
# platform :ios, '6.0'

use_frameworks!

target 'HttpRequest-iOS' do
    pod 'JsonSerializer', :path => '~/Developer/iOS/Skyvive/JsonSerializer'
end

target 'HttpRequest-OSX' do
    pod 'JsonSerializer', :path => '~/Developer/iOS/Skyvive/JsonSerializer'
end

target 'HttpRequestTests' do
    pod 'JsonSerializer', :path => '~/Developer/iOS/Skyvive/JsonSerializer'
end
